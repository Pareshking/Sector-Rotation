# Runtime only. Dev tooling lives in requirements-dev.txt so the deployed app
# does not build scipy, pytest and ruff on every restart.
streamlit>=1.45,<2
pandas>=2.2,<3
numpy>=2.0,<3
pyarrow==22.0.0
pydantic>=2.8,<3
plotly>=5.24,<7
requests>=2.32,<3
jugaad-data==0.35.5
yfinance>=0.2.54,<1
